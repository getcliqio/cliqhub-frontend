# cliqhub-frontend

Vite + React SPA for CliqHub.

Not deployed alone — **cliqhub-bff** builds this repo and serves `dist/` as `/app/spa`.

```bash
npm ci && npm run dev
npm run build && npm test
```
